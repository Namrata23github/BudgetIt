/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MGTabBarContainer from './app/Containers/MGTabBarContainer.js'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MGTabBarContainer);
